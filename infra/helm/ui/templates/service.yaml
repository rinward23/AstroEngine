apiVersion: v1
kind: Service
metadata:
  name: {{ include "astroengine-ui.fullname" . }}
  labels:
    {{- include "astroengine-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "astroengine-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
